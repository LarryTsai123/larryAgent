Please read `AGENTS.md` first.

Repository policy:
- Treat `aidelivery/governance/` as repository-wide constraints
- Treat `aidelivery/capabilities/*/contract.yaml` as execution contracts
- Do not invent runtime order from prose when a workflow instance exists
- Prefer updating evidence and trace outputs when delivery work changes
