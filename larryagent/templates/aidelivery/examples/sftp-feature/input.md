# SFTP Settings Feature

Add a single-record SFTP settings management feature.

## Requirement
- Add a Resources page for editing one SFTP configuration.
- Provide backend CRUD endpoints.
- Persist the data to a JSON file instead of a database.
- Include validation, integration test, and runtime validation.
